# Desktop Wrapper

This Electron wrapper is intended for heavy local workflows where browser memory, permission prompts, or worker throttling become limiting factors.

## Run

1. Build the web app:
   `npm run build`
2. Install desktop dependencies:
   `cd desktop && npm install`
3. Start Electron:
   `npm run dev`
